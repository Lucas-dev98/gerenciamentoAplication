PORT=3001
# Para usar MongoDB local, certifique-se de que o MongoDB está rodando
# DB_URI=mongodb://localhost:27017/epu_gestao

# Para usar MongoDB Atlas (gratuito), substitua pela sua string de conexão
# ou use esta temporária para testes (pode não funcionar)
# DB_URI=mongodb://localhost:27017/epu_gestao

# Usar repositório JSON para desenvolvimento sem MongoDB
NODE_ENV=development-json
JWT_SECRET=your_jwt_secret_key_change_this_in_production